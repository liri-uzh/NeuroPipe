# NeuroPipe

Creating virtualenv (neuropipe-env)
```bash
python3.11 -m venv neuropipe-env
source neuropipe-env/bin/activate
```

Upgrading pip and installing packages
```bash
pip install --upgrade pip
pip install -r requirements.txt
```